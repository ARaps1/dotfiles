# Pi Agent Harness configuration
# Auto-sourced in dev-in-docker (via ZSH_CUSTOM)
# and sourced from .zshrc on personal machines.

# Pi uses dev.ai-inference AWS profile for its LLM calls
alias pi='AWS_PROFILE=dev.ai-inference pi'
